import styled from "styled-components";

export const ArrowStyled = styled.div`
  position: absolute;
  top: -92px;
  right: 78px;
  z-index: 20;

  svg {
    fill: transparent;
    stroke: var(--main-dark);
  }
`;
